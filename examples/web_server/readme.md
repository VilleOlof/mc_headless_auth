a simple example on how to use the crate along side a web server.  
it automatically starts the minecraft server on the default port.  
then a website at `http://localhost:8000` where you can input a token given from the game.  
using this the website displays the player data if its a valid token.  